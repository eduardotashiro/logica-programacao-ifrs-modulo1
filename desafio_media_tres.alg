programa {
    funcao inicio() {
        real n1, n2, n3, media
        escreva("Digite três números: ")
        leia(n1)
        leia(n2)
        leia(n3)
        media = (n1 + n2 + n3) / 3
        escreva("A média é: ", media)
    }
}